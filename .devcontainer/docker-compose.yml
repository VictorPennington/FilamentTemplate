name: FilamentTemplate
services:
  app:
    image: filament-app:latest
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/var/www/html:cached

  # db:
  #   image: mysql:8.4.2
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpassword
  #     MYSQL_DATABASE: formapp_db
  #     MYSQL_USER: formapp_user
  #     MYSQL_PASSWORD: formapp_password
  #   ports:
  #     - "3331:3306"
  #   volumes:
  #     - db_data:/var/lib/mysql


  # phpadmin:
  #   container_name: phpmyadmin
  #   image: phpmyadmin
  #   restart: always
  #   environment:
  #     PMA_ARBITRARY: 1
  #     PMA_HOST: db
  #     PMA_USER: root
  #     PMA_PASSWORD: "${DB_PASSWORD}"
  #     UPLOAD_LIMIT: 64M      
  #   ports:
  #     - "8080:80"    
